<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>de.kugidev</groupId>
    <artifactId>battleoftheuniverse</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>Battleoftheuniverse</name>
    <description>WarOfGalaxy-style browser game backend</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>25</java.version>
        <spring-modulith.version>2.1.0</spring-modulith.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <jacoco.version>0.8.15</jacoco.version>
        <pitest.version>1.30.0</pitest.version>
        <pitest-junit5.version>1.2.3</pitest-junit5.version>
        <!-- Enforced by the jacoco:check goal bound to verify (see below); bump alongside real coverage gains. -->
        <jacoco.instruction.minimum>0.80</jacoco.instruction.minimum>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-events-api</artifactId>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-liquibase</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-actuator</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>

        <!-- JSON Schema generation (from the catalog module's Java records) and validation -->
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-generator</artifactId>
            <version>5.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.networknt</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>3.0.7</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Spring Boot 4's modularization moved MockMvc test-slice support (@AutoConfigureMockMvc)
             out of spring-boot-test-autoconfigure into its own per-web-stack starter. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
    </dependencies>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.modulith</groupId>
                <artifactId>spring-modulith-bom</artifactId>
                <version>${spring-modulith.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
              Security patch overrides: these transitive versions are pinned by the
              spring-boot-dependencies BOM (imported via spring-boot-starter-parent) to
              releases with known, fixed CVEs. Bumped ahead of the BOM here rather than
              waiting for the next Spring Boot release; see SECURITY.md for the analysis.
            -->
            <dependency>
                <!-- CVE-2026-54291: pgjdbc channel-binding auth downgrade, fixed in 42.7.12. -->
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>42.7.13</version>
            </dependency>
            <dependency>
                <!-- CVE-2026-49844: log4j-api MapMessage JSON float encoding, fixed in 2.25.5. -->
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-api</artifactId>
                <version>2.25.5</version>
            </dependency>
            <dependency>
                <!-- GHSA-5gvw-p9qm-jgwh: jackson-databind @JsonView bypass, fixed in 3.1.5.
                     Bumped alongside jackson-core/jackson-dataformat-yaml to keep the
                     tools.jackson 3.1.x release train aligned. -->
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>3.1.5</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>3.1.5</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.dataformat</groupId>
                <artifactId>jackson-dataformat-yaml</artifactId>
                <version>3.2.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <showWarnings>true</showWarnings>
                    <compilerArgs>
                        <arg>-Xlint:all</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>0.2.0</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>de.kugi.dev.battleoftheuniverse.BattleOfTheUniverseApplication</mainClass>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!--
              Coverage. `lombok.config` (repo root) makes Lombok tag generated getters/setters
              with @Generated, which JaCoCo excludes automatically - without that, boilerplate
              accessors on the many @Getter/@Setter entities would dilute the ratio with noise
              that no test could meaningfully "cover" beyond exercising the field once.
              `mvn verify` runs the check; `target/site/jacoco/index.html` is the human report.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <excludes>
                                        <exclude>de.kugi.dev.battleoftheuniverse.BattleOfTheUniverseApplication</exclude>
                                        <exclude>de.kugi.dev.battleoftheuniverse.*.dto.*MapperImpl</exclude>
                                        <!-- Dev-profile-only NPC/account seeding convenience, never runs in prod; not business logic. -->
                                        <exclude>de.kugi.dev.battleoftheuniverse.DevWorldSeeder</exclude>
                                        <exclude>de.kugi.dev.battleoftheuniverse.user.DevAccountSeeder</exclude>
                                        <exclude>de.kugi.dev.battleoftheuniverse.user.DevAccountsProperties</exclude>
                                        <exclude>de.kugi.dev.battleoftheuniverse.user.DevAccountsProperties.*</exclude>
                                    </excludes>
                                    <limits>
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.instruction.minimum}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
              Mutation testing ("does the test suite actually catch bugs, or just execute lines").
              Scoped to *Service/*Scheduler/business-logic classes rather than the whole codebase -
              mutating generated mappers, DTOs, and Lombok accessors produces only noise/equivalent
              mutants. Not bound to a lifecycle phase - it's slow, so it's an opt-in gate (e.g. in CI
              or before a release), not part of every `mvn verify`. Report: target/pit-reports/index.html.

              Run:
                mvn clean test-compile org.pitest:pitest-maven:mutationCoverage
              pitest-maven ${pitest.version} bundles a Java-25-aware ASM (bumped to 9.10.1 in 1.25.6),
              so it reads this project's Java 25 (class file 69) bytecode directly - no release
              downgrade needed (earlier versions pinned to 1.19.1 required one; see git history).
            -->
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>${pitest.version}</version>
                <configuration>
                    <targetClasses>
                        <param>de.kugi.dev.battleoftheuniverse.*.*Service</param>
                        <param>de.kugi.dev.battleoftheuniverse.*.*Scheduler</param>
                        <param>de.kugi.dev.battleoftheuniverse.*.CombatService</param>
                        <param>de.kugi.dev.battleoftheuniverse.*.SystemLayout</param>
                        <param>de.kugi.dev.battleoftheuniverse.catalog.ResourceCost</param>
                    </targetClasses>
                    <targetTests>
                        <param>de.kugi.dev.battleoftheuniverse.*</param>
                    </targetTests>
                    <mutators>
                        <mutator>DEFAULTS</mutator>
                    </mutators>
                    <timestampedReports>false</timestampedReports>
                    <threads>4</threads>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-junit5-plugin</artifactId>
                        <version>${pitest-junit5.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>
