spring:
  datasource:
    url: jdbc:h2:mem:battleoftheuniverse-test-${random.uuid};DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

game:
  catalog:
    # Under target/ (auto-cleaned, gitignored) - never the real ./config/catalog a dev server also reads/writes.
    dir: target/test-catalog-${random.uuid}
